export namespace CALC.struct {
  export class Rectangle {
    public x: number;
    public y: number;
    public weight: number;
    public height: number;

    consutuctor(x: number, y: number, weigth: number, height: number) {
      this.x = x;
      this.y = y;
      this.weigth = weigth;
      this.height = height;
    }
  }
}
